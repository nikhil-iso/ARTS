; Standalone hardware test for the BMP280 pressure and temperature sensor.
; Open this folder in PlatformIO, then build and upload to the Teensy 4.1.

[env:teensy41]
platform = teensy
board = teensy41
framework = arduino
lib_deps = adafruit/Adafruit BMP280 Library@^3.0.0
